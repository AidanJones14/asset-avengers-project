# Copy this file to `.env` in the backend folder and replace the values.

DB_URL=jdbc:postgresql://localhost:5555/asset-avengers-client
DB_USERNAME=postgres
DB_PASSWORD=replace_with_your_real_password
DB_DRIVER_CLASS_NAME=org.postgresql.Driver

